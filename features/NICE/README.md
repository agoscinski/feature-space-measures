`librascal` is required to use nice https://github.com/cosmo-epfl/librascal
Install nice and run the `compute_nice_features.py` script.

    git clone https://github.com/cosmo-epfl/nice
    cd nice
    pip install -e .
    cd ..
    python compute_nice_features.py
